# CodexStyle 项目规则

本文件适用于 `codexStyle/` 及全部子目录。

`REQUIREMENTS.md` 与 `PLAN.md` 是本轮已审核需求和架构契约的唯一事实来源。`docs/superpowers/**` 等并行或历史设计草稿仅供背景参考；发生冲突时不得据其恢复联网更新、旧版无 CSS 导入、现有会话热应用或不同 IPC 命名。

## 范围与安全边界

- 新代码只能写入 `codexStyle/`；`../old/` 仅可只读参考，禁止修改、移动或删除。
- 产品仅支持 Windows x64 Electron 桌面端、本地主题库、离线 Studio、系统托盘和本工具拥有的 Codex 会话。
- 不实现在线 Gallery、登录、投稿、云同步、网页一键换肤或 `dreamskin://`。网络仅用于用户主动触发的 CodexStyle 正式安装版更新；禁止后台轮询、静默下载、静默安装、任意更新源或更新官方 Codex。
- 不修改 WindowsApps、ACL、`app.asar`、官方签名或系统执行策略；不要求管理员权限。
- 不直启受保护的 `ChatGPT.exe` 作为参数回退。只通过当前用户注册的官方 `OpenAI.Codex` AppX/AUMID 启动。
- 外部启动的 Codex 会话绝不注入、关闭、重启或附着。身份不完整、CDP 不可用或选择器不兼容时 fail closed。
- CDP 仅允许字面 `127.0.0.1`，并必须验证 AppX 身份、PID、进程开始时间、启动 nonce、监听 PID、端口和 Browser ID。
- 自更新只允许 Windows x64 NSIS 正式安装版访问固定的 `https://github.com/Zhqiankun/codexDream/releases/latest/download/` 更新源。开发版、ZIP 便携版、非 Windows 平台或缺少安装标记时必须明确返回不支持。
- 更新检查与下载必须由用户显式触发；下载的安装包必须由 `electron-updater` 按同一次构建生成的 `latest.yml` SHA-512 校验通过后才进入可安装状态。安装前必须先经现有 operation gate 安全结束本工具拥有的 Codex 会话，失败则不退出、不安装。
- 更新端点、版本、安装路径和下载文件路径不得由 renderer 提供。安装包未签名期间必须明确展示未知发布者风险；一旦启用代码签名，更新校验必须锁定发布者身份。
- 主进程诊断日志位于 Electron `userData/logs`，不属于 secure-store 保护域。日志只能记录事件名、版本、错误码、校验字段路径与经过脱敏截断的错误消息；禁止记录主题/CSS/JSON/图片内容、对话内容、完整命令行、nonce、令牌、密钥或 URL 查询参数。日志按日轮换、单文件有界、默认保留 7 天，并在启动及每 24 小时清理；日志失败不得改变应用控制流。renderer 只能通过固定 IPC 让系统打开日志目录，不得获得或提供任意路径。
- Windows x64 受管数据必须经仓库内源码构建的 N-API secure-store 模块访问。受保护域固定为 `%LOCALAPPDATA%\\CodexStyle` 下的预定义相对 managed path；禁止把任意路径、绝对路径、用户输入路径或通用文件系统 API 暴露给该模块。
- secure-store 在主进程生命周期内持有已验证根目录句柄，所有受管目录与文件必须逐段使用 handle-relative NT I/O 打开。每段均以 `FILE_OPEN_REPARSE_POINT` 语义打开并拒绝任意 reparse point；路径、类型、句柄或原子提交出现任何异常都必须返回 `STORE_TAMPERED`。
- `state/`、`themes/`、`transactions/`、`lock/` 与 `ownership/` 全部属于 secure-store 保护域，禁止 Node `fs` fallback。用户通过原生保存对话框选择的导出 ZIP 位于保护域之外，继续使用现有有界导出与 ZIP 校验流程。
- 受管原子写固定为同目录受控临时文件、写入、flush、再通过 handle-relative rename 提交。native `.node` 必须 ASAR-unpacked；生产包缺失、加载失败或架构不匹配时 fail closed，禁止回退到 Node `fs`。

## 架构与依赖方向

依赖方向固定为：

```text
renderer -> preload bridge -> main IPC -> application/domain ports -> infrastructure
```

- renderer 开启 sandbox 和 context isolation，不得使用 Node、Electron、文件系统、PowerShell、CDP 或 main 私有模块。
- renderer 只能类型导入 `src/contracts/`，业务调用必须经 `src/renderer/api/bridge.ts`。
- preload 只暴露白名单强类型 API，不暴露通用 `invoke`、路径、PID、端口或 nonce。
- domain 不依赖 Electron、PowerShell、React 或具体存储实现。
- domain 与 application 只能依赖 secure-store 的受限端口，不得直接依赖 N-API binding 或 Node `fs`；native binding 只能由 main infrastructure adapter 加载。
- main 不导入 renderer 页面或组件。
- 不新增 `common`、`shared`、`utils` 或 `helpers`。唯一跨层公开模块是 `src/contracts/`。
- 页面和 IPC handler 只做编排、协议转换、校验和响应组织；主题规则、会话状态机和数据事务放在各自领域或应用服务。

## 数据与契约

- 数据根目录固定为当前 Windows SID 的 `%LOCALAPPDATA%\\CodexStyle`；renderer 不得获得真实路径。
- `ThemeRepository` 拥有主题、草稿、指纹、导入事务和 last-known-good。
- `SelectionService` 拥有下次工具启动时的主题选择与暂停标记。
- `CodexSessionService` 拥有工具启动会话的 nonce、PID/开始时间、包身份、CDP 端口和 Browser ID。
- IPC 协议版本为 `v: 2`，renderer-ready 启动握手保留固定 `v: 1` bootstrap 以识别覆盖安装后的驻留旧主进程；其他请求必须严格匹配当前协议。所有响应使用 `Result<T>`；handler 必须验证 sender、frame URL、Zod schema、大小上限和单用户操作锁。
- 主题 ZIP、Safe CSS 和图片必须在导入、保存 ready、选择应用和实际注入前按 `REQUIREMENTS.md` 复验。

## 文件责任

- 研发协调员：`.agents.md`、`REQUIREMENTS.md`、`PLAN.md`。
- 后端架构师：根构建配置、`src/contracts/**`、`src/main/**`、`src/preload/**`、`tests/main/**`、`tests/fixtures/**`。
- secure-store 阶段的后端独占文件：`native/secure-store/**`、native 构建配置、`src/main/infra/secure-store/**`、受影响的 `src/main/infra/local-store.ts`、`src/main/session/session-service.ts`、`src/main/app/controller.ts`、根打包/构建/验证配置及必要 `tests/main/**`、`tests/fixtures/**`。不得修改 `src/contracts/**`、`src/preload/**`、`src/renderer/**` 或 `../old/**`。
- 前端架构师：`src/renderer/**`、`resources/icons/**`、`tests/renderer/**`。
- 测试工程师：只读检查全部 diff；可新增或修改 `tests/acceptance/**`，不得代替实现角色修复产品代码。
- 同一文件同一阶段只能有一个写入责任人。契约变更必须先由后端更新 `src/contracts/` 并同步前端和测试。

## 提交、推送与版本发布

- 每次准备向远端推送前，必须先询问用户：“这次是否构建并发布新版本？”除非用户在当前请求中已经明确说明是否发布，否则不得直接推送。
- 用户选择“不发布”时，只推送当前分支提交；不得创建或推送版本标签，也不得手动创建 GitHub Release 或上传本地产物。
- 用户选择“发布”时，必须先确认目标版本号，并确保它是稳定的 SemVer `X.Y.Z`；`package.json`、`package-lock.json`、`CHANGELOG.md` 与发布说明中的版本必须一致。
- 发布标签固定为 `vX.Y.Z`，必须指向包含对应版本代码与发布元数据的确切提交。提交推送成功后，自动创建注释标签并推送该标签，以触发 `.github/workflows/release.yml`。
- 创建标签前必须同时检查本地标签、远端标签和 GitHub Release。若同名标签或 Release 已存在，禁止移动、覆盖、删除或强制推送；必须停止并询问用户如何处理。
- 发布产物只能由 GitHub Actions 构建、校验和上传。不得以维护者电脑上的本地产物替代 Actions 产物。
- GitHub Release 必须同时发布 NSIS 安装包、ZIP、`latest.yml`、NSIS blockmap 和 `SHA256SUMS.txt`；`latest.yml` 与安装包必须来自同一次 Actions 构建并通过 SHA-512 一致性校验。
- 标签推送后，必须确认对应的 Release Actions 已由标签事件触发，并向用户报告运行链接与当前状态；Actions 失败时不得把发布报告为成功。
- 禁止对 `main` 或版本标签执行强制推送。发布所需验证未通过、版本不一致或工作区包含范围不明的改动时，必须停止发布并说明原因。

发布流程固定为：

```text
确认是否发布新版本
  -> 确认并校验 X.Y.Z
  -> 完成验证与发布提交
  -> 推送当前分支
  -> 创建并推送注释标签 vX.Y.Z
  -> 确认 GitHub Actions 标签构建已触发
  -> 等待并报告构建、校验和 Release 结果
```

## 验证与报告

适用时运行：

```powershell
npm ci
npm run format:check
npm run lint
npm run typecheck
npm run test:unit
npm run test:renderer
npm run test:integration
npm run test:e2e
npm run build
npm run package:win
npm run verify:package
```

执行角色完成时必须报告：修改文件、新增模块和职责、共享抽象、依赖变化、实际命令与结果、未验证项和剩余风险。不得以自报通过代替测试工程师的独立证据。
